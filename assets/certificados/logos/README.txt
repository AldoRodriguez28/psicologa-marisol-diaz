Guarda aquí los logos de las instituciones que emiten cada certificado.

Sugerencias de nombre:
- issuer-uni.png, issuer-fed.png, issuer-academy.png, issuer-instituto.png

Recomendación de tamaño:
- Alto 24–28px, fondo transparente. PNG o SVG.

Uso:
- En `index.html`, sección `#certificaciones`, ajusta la ruta `src` y el texto del `<em>` bajo cada `.issuer`.

